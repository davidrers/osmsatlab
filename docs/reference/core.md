# Core System

The `OSMSatLab` class is the main entry point for the library. It orchestrates data fetching, processing, and analysis.

::: osmsatlab.core.OSMSatLab
    options:
      members:
        - calculate_heat_access_vulnerability
        - calculate_heat_exposure
        - calculate_accessibility_metrics
        - calculate_per_capita_metrics
        - load_satellite_data
        - fetch_services
